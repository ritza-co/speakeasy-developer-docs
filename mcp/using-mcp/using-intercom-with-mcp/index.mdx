---
title: "Using Intercom with MCP"
description: "Connect Intercom to Claude Desktop to analyze customer conversations and monitor customer health"
---

import { Callout, Screenshot } from "@/mdx/components";
import GramCallout from "../.partials/gram-callout.mdx";

<GramCallout />

You're checking [Intercom](https://www.intercom.com/) at 11PM again. Scrolling through conversations, trying to figure out which customers are actually happy and which ones are quietly planning their exit. Sound familiar?

<Screenshot src="assets/intercom-inbox-demo.png" alt="Intercom inbox showing multiple conversations with varying customer health signals" />

Looking at this inbox, can you quickly tell which customers need immediate attention? There's James Wilson asking about cancellation, Will Duncan reporting repeated technical failures, and Adam expressing extreme frustration. Meanwhile, David seems happy with new features, but how do you prioritize your response?

Customer success teams waste hours manually checking conversation sentiment, writing generic follow-up emails, and missing churn signals until it's too late. Meanwhile, all the data needed to predict and prevent churn is sitting right there in your Intercom conversations.

## The Solution: Give Claude the ability to analyze Intercom conversations

The Intercom MCP server connects your Intercom data to an MCP client like Claude Desktop. Once connected, you can ask Claude to analyze your customer conversations and spot patterns you'd miss manually.

With the Intercom MCP server, Claude can:

- **Analyze support conversations** for sentiment and engagement patterns
- **Identify early churn signals** before customers complain
- **Generate personalized outreach** based on conversation history
- **Create weekly health reports** with actionable insights

Customer success teams save 8-10 hours per week while improving retention rates.

## Prerequisites

Before we begin, make sure you have:

- [Claude Desktop](https://claude.ai/download) installed
- An Intercom workspace with admin access or permission to create integrations
- A few sample conversations in Intercom (we'll show you how to set this up)

## Set Up Demo Data in Intercom

For this guide, we need realistic customer data to analyze. We'll use the Intercom Messenger chat widget to create sample conversations.

### Install the Intercom Messenger Component

Since we can't directly create conversations in Intercom, we'll use a Messenger widget component to create sample conversations. 

<Screenshot src="assets/intercom-messenger-install.png" alt="Intercom Messenger installation page showing code snippet options" />

Navigate to **Settings** > **Channels** > **Messenger** in your Intercom workspace. Under **Install** you'll see installation options for different platforms. Choose "Code snippet" and follow the instructions to install the widget.

### Create Sample Customer Conversations

Using the Messenger widget, we'll create several conversations that represent different customer health scenarios.

These messages will range from satisfied customers asking feature questions to frustrated users reporting technical issues and customers requesting cancellations. Here's an example of what one message might look like:

<Screenshot src="assets/intercom-conversation-example.png" alt="Example conversation showing a customer requesting subscription cancellation" />

James Wilson is a customer who is actively seeking to cancel his subscription due to poor ROI. This is a critical churn signal that needs immediate attention.

We'll create a few more messages with varying sentiment levels to build a realistic idea of what a messy inbox looks like.

### The Problem with Manual Customer Health Analysis

Once you've created these messages, your Intercom inbox should look like this:

<Screenshot src="assets/intercom-final-inbox.png" alt="Intercom inbox showing multiple conversations with varying customer health signals" />

Now here's the challenge: looking at this conversation list, can you quickly tell which customers are healthy and which ones are at risk of churning? 

You'd need to:
- Click into each conversation individually
- Read through the entire message history
- Try to remember the context from previous conversations
- Manually assess the sentiment and urgency
- Keep track of response times and escalation patterns
- Cross-reference with customer data from other systems

For just three customers, this might take 15-20 minutes. Imagine doing this for 50+ customers every week. That's where the Intercom MCP server comes in.

## Install and Configure the Intercom MCP Server

We'll use the Intercom MCP server to connect your Intercom data to Claude Desktop.

### Get Your Intercom Access Token

First, create an Intercom app to access your data:

1. In Intercom, go to **Settings** > **Integrations** > **Developer Hub**
2. Click **New App** and create an internal integration
3. Name it "Customer Health Monitor"
4. Under **Authentication**, you'll need to configure permissions for the MCP server to access your data
5. Enable the following permissions:
   - Read conversations
   - Read contacts  
   - Read companies
   - Read events
   - Write events (for updating customer data)
6. Copy your **Access Token** from the authentication section

### Configure Claude Desktop

Next, we need to configure the Intercom MCP server in Claude Desktop.

Open your Claude Desktop configuration file and add the Intercom MCP server:

- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`  
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`

```json
{
  "mcpServers": {
    "intercom": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://mcp.intercom.com/mcp",
        "--header",
        "Authorization:${AUTH_HEADER}"
      ],
      "env": {
        "AUTH_HEADER": "Bearer YOUR_INTERCOM_ACCESS_TOKEN"
      }
    }
  }
}
```

Replace `YOUR_INTERCOM_ACCESS_TOKEN` with the access token you copied from the Intercom authentication page in the previous step.

Restart Claude Desktop. When you first try to use the Intercom tools, Claude will ask for permission to access them. Click "Allow always" or "Allow once" to proceed.

## Analyze Customer Health with Claude

With the Intercom MCP server configured, you can now ask Claude to analyze your customer conversations. Test the integration with:

> List my recent Intercom conversations and identify any customer health signals. Summarize and give me a list of customers that are at risk of churning.

<video
  controls={false}
  loop={true}
  autoPlay={true}
  muted={true}
  width="100%"
  className="mx-auto my-10 max-w-6xl"
>
  <source src="assets/intercom-claude-desktop.mp4" type="video/mp4" />
</video>

Claude will use the Intercom MCP server to get the list of conversations, identify the conversations that are at risk of churning, and summarize the conversations.

## Automate Your Workflow

Once you've gotten the hang of it, you can automate it by:

1. **Creating a weekly reminder** to run the health check
2. **Setting up templates** for common outreach scenarios  
3. **Building a simple dashboard** to track health score trends over time

Ask Claude:

> Create a follow-up workflow for next week. Set a reminder to run this analysis again and track how our outreach affected the health scores.

## Getting started with MCP

If you're considering MCP for your organization, [MCP.so](https://mcp.so/) has an extensive list of MCP servers and clients, including servers that may integrate with your existing customer success tools.

To learn more about MCP and how to try it out, check out our [quickstart guide to installing MCP servers](/mcp/getting-started/quickstart).

If you have any questions or feedback about this guide, or want to share your own experiences with customer health monitoring using MCP, [please join us on Slack](https://go.speakeasy.com/slack).

To find out how we can help you generate your own MCP servers, [get in touch](https://www.speakeasy.com/contact) on our website.